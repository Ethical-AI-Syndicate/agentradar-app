{
  "version": 2,
  "installCommand": "cd web-app && npm install --production=false",
  "buildCommand": "cd web-app && npm run build",
  "outputDirectory": "web-app/.next",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./web-app",
  "env": {
    "NODE_ENV": "production"
  }
}